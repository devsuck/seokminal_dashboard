"use client";

import AutoResearchPanel from "@/components/AutoResearchPanel";

/* 독립 URL 보존(사이드바 은퇴 — AI LAB에 흡수됨). 배치 뷰 = AutoResearchPanel. */
export default function AutoResearchPage() {
  return <AutoResearchPanel />;
}
